using Microsoft.AspNetCore.SignalR;

namespace EaproERP.Hubs
{
    // This is the gatekeeper for live factory data
    public class FactoryHub : Hub 
    { 
        // We don't need methods here yet; it just needs to exist
    }
}